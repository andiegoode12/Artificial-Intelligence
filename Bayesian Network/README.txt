Files:
  enumeration.java

To compile:
  javac enumeration.java
  java enumeration

Input Examples: (Do not include spaces and check spelling)
  Example inputs for "What is your query and its outcome?"
    pollution=high
    xray=positive

  Example inputs for "What is the evidence and its outcome(s)?('exit' when finished)"
  Insert each piece of evidence separately. When finished type in "exit".
    dyspnea=true
    xray=positive
    cancer=true
